import Ember from 'ember';

export default Ember.Route.extend({
  model() {
    return ['bekah needs to get more banana chips', 'josh needs to do a cartwheel', 'Bekah needs to get out of her funk', 'We both need to listen to some funky music', 'make money']
  }
});
